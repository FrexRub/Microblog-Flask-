tags:
  - medias
summary: User Follow
description: User Follow
operationId: user following
parameters:
  - name: api-key
    in: header
    required: true
    schema:
      type: string
      title: Api-Key
requestBody:
  required: true
  content:
    multipart/form-data:
      schema:
        $ref: '#/definitions/Body_post_medias'
definitions:
    Body_post_medias:
      properties:
        file:
          type: string
          format: binary
          title: File
      type: object
      required:
        - file
      title: Body_post_medias

    MediaOut:
      properties:
        rusult:
          type: boolean
          title: Response status
        media_id:
          type: integer
          title: Media ID
      type: object
      required:
        - rusult
        - media_id
      title: MediaOut

    ValidationError:
      properties:
        error_message:
          type: string
          title: Error Message
        error_type:
          type: string
          title: Error Type
        rusult:
          type: boolean
          title: Response status
      title: ValidationError
responses:
  201:
    description:
    schema:
      $ref: '#/definitions/MediaOut'
  418:
    description:
    schema:
      $ref: '#/definitions/ValidationError'